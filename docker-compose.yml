version: '3.8'

services:
  telegram-userbot:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ./data:/app
    command: >
      sh -c "
        pip install --no-cache-dir telethon requests &&
        python bot.py
      "
    restart: unless-stopped
